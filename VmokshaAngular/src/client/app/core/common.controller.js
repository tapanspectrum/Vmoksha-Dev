/* Description: The Common controller which is applicable throughout application.
 * Author: 
 * Created On: 
 * Modified For: 
 * Modified On:
 * Modified By:
 * */

(function () {
    'use strict';

    angular
        .module('app.core')
        .controller('Common', Common);

    /* @ngInject */
    function Common($state, routerHelper, logger, common, config, _, $sce, $rootScope, $scope, $timeout, $document, $log, toastr, store, $injector, vmuisettings) {
		
		
    }
})();
